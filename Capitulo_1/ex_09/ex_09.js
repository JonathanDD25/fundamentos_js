/* ARRAYS O ARREGLOS
un array es una estructura de datos que se utiliza para almacenar una coleccion ordenada de elementos.
estos elementos pueden ser cadenas de texto, valores numericos como: enteros, decimales; valores booleanos, entre otros. pueden ser tambien un conjunto de funciones e inclusive otro arrays

*/

let numeros = []; // declaracion de un array vacio
numeros = [15, 80, 650, 50.43, -10]; // se guarda en el array una coleccion numerica de tipo decimal o entero
console.log(numeros); // se imprime por consola todo el array
console.log(numeros[3]); // se imprime por consola el 4to elemnto que pertenece al indice 3